{
  "manifest_version": 3,
  "name": "Time Tracker — URL Reporter",
  "version": "1.0",
  "description": "Reports the active browser tab's URL to your local time tracker (localhost only).",
  "permissions": ["tabs", "alarms"],
  "host_permissions": ["http://localhost:7879/*", "http://127.0.0.1:7879/*"],
  "background": { "service_worker": "background.js" }
}
